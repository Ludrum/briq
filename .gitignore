# --- kept local, deliberately not published --------------------------------
# Product and design rationale. Still on disk, just not in the public repo.
docs/PRODUCT.md
docs/DESIGN.md

# The printable brick object. Unfinished - not published yet.
hardware/

# --- secrets and machine-local config -------------------------------------
# The real ones. Each has a committed .example next to it.
android/briq.properties
android/local.properties
pi/clients.conf
pi/controller.env
pi/nftables.conf

# Generated by pi/gen-qr.py. Every QR code on this sheet contains the master
# token in plain form, so the sheet IS the credential. Print it, do not
# commit it.
sheet.html
qr/
*.qr.png

# --- Android build output --------------------------------------------------
android/.gradle/
android/build/
android/app/build/
android/captures/
android/.cxx/
*.apk
*.aab
*.jks
*.keystore

# --- Python ----------------------------------------------------------------
__pycache__/
*.py[cod]

# --- editors / OS ----------------------------------------------------------
.idea/
*.iml
.DS_Store
Thumbs.db
desktop.ini
android/.kotlin/
